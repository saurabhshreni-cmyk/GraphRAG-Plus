# Backend base URL. In dev this is the local FastAPI server.
# In production, set this in the Vercel project settings to your deployed
# backend (e.g. https://graphrag-plus.onrender.com).
VITE_API_BASE=http://127.0.0.1:8765
